node_modules
dist

# Environment files
.env
.env.local
.env.production
.env.staging

# Build outputs
.vercel

# Claude Code
.claude/

# Test and coverage outputs
coverage/
test-results/

# cc-sessions runtime files
sessions/sessions-state.json
sessions/package.json
sessions/transcripts/
sessions/.archived/

# Compiled JS files in src/ (we keep TS/TSX sources only)
# This prevents committing build artifacts alongside source files
src/**/*.js
src/**/*.jsx

# Keep source-only JS files (add exceptions here if needed)
# !src/path/to/important.js
